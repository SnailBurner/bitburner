

{
    let weakness = false;
    let richness = false;
     while(true)
    {
        weakness = (ns.getServerSecurity(target) == ns.getServerMinSecurity(target));
        richness = (ns.getServerAvailableMoney(target) == ns.getServerMaxMoney(target));
        if (weakness && richness)
    
    
    }




}
